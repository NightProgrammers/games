{
  "name": "sticker-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:app": "node --check src/app.js",
    "verify": "npm test && npm run check:app"
  }
}
